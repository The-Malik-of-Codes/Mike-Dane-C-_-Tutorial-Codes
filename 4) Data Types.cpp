#include <iostream>

using namespace std;

int main ()
{
	char grade = 'A'; // char allow us to store a single character must be stored in a "('')" 
	//it important to note this.
	string pharse = "The Malik of Codes"; // this are for strings.
	int age = 50; // this is to store a whole number.
	int ages = -30 ;// to store a negative whole number
	float  num = 16.39; // to store a decimal numbers with less or few decimal point.
	double gpa = 2.75 ;// to sore a decimal numbers with more decimal point. thhis is used mostly,
	bool isMale = true ;
	
	cout << grade << endl;
	cout << "This is the Malik of Codes," << endl;
	
	return 0;
}